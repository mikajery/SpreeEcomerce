<%= form_for([:admin, @client_account] ) do |f| %>
  <% if @client_account.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@client_account.errors.count, "error") %> prohibited this client_account from being saved:</h2>

      <ul>
      <% @client_account.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :credit_duration %><br>
    <%= f.number_field :credit_duration %>
  </div>
  <div class="field">
    <%= f.label :credit_limit %><br>
    <%= f.number_field :credit_limit %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
